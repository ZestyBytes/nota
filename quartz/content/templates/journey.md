---
title: ""
type: journey
journey: ""
day: 1
tags: []
occurredAt: "YYYY-MM-DD"
createdAt: "YYYY-MM-DD"
publishedAt: "YYYY-MM-DD"
publish: true
---

Write today's entry in the journey. Give every entry in the same undertaking
the same `journey:` name and its own `day:` number, and they thread together
into one page in day order, reachable from Library, Journeys.

If the journey is heading for a number, add `start:`, `target:` and `unit:` to
the first entry and a `metric:` to every check-in, and the journey gets a
progress bar. Without those it gets a dot per entry instead.
